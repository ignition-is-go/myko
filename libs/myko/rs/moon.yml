$schema: 'https://moonrepo.dev/schemas/project.json'
language: 'rust'
stack: 'backend'
layer: 'library'
tags:
  - 'rust-typegen'
tasks:
  gen:
    deps:
      - '~:clean'
      - 'root:install'
  gen-watch:
    deps:
      - 'root:install'
