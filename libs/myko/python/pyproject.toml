[tool.poetry]
name = "myko"
version = "0.0.3"
description = "python tooling for the myko protocol"
authors = ["Trevor Sargent <trevor@lucid.rocks>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
asyncio = "^3.4.3"
websockets = "^12.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
