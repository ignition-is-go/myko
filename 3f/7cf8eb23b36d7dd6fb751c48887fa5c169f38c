�ditem�dbodyy%MIGRATION LIST IS FIVE ITEMS, NOT TWO. The room converged on 'exactly two edits' — that is correct for HARD BREAKS but undercounts. Verified by manifest grep (grep -rn 'hyphae/' --include='*.toml') plus a read of every hyphae-related comment.

HARD BREAKS (resolve errors on ^2.0):
1. libs/myko/server/Cargo.toml:71  trace = ['hyphae/trace']  -> delete. 'trace' ceases to exist in hyphae 2.0.
2. libs/myko/server/Cargo.toml:80  ci = ["profiling", "trace"]  -> drop "trace". References (1), so it breaks with it.

STALE-BUT-COMPILING (the 'configuration that looks live and isn't' class the release-train room identified — these mislead rather than fail, which is worse):
3. Cargo.toml:83-93 — the workspace hyphae comment block documents the 1.3.4 cost split in detail ('trace carries the MEMORY... profiling adds only hot-path CPU on top... trace is the whole measured overhead'). After 2.0, trace/metrics do not exist, and hyphae's corrected benchmark puts the surviving profiling feature at 46.4-48.3ns vs 47.3-48.1ns baseline — INDISTINGUISHABLE. So the block would actively deter someone from enabling the one surviving diagnostic on cost grounds that no longer apply. It is scoped 'in hyphae 1.3.4' so it reads as history, but the operative guidance needs rewriting: keep 'no observability features here, they unify across the graph', drop the cost taxonomy.
4. libs/myko/server/Cargo.toml:66-68 — the profiling NOTE asserts 'profiling implies hyphae's trace/metrics (the per-cell registry and CellMetrics — the memory) on top of its own hot-path cost'. Flatly false after 2.0 on both halves.
5. libs/myko/server/Cargo.toml:72-77 — the inspector NOTE describes hyphae's inspector in the present tense ('is an in-process tokio+serde_json web server'). hyphae deletes it entirely in 2.0, so it should read as past tense / why-there-is-no-feature.

Note: items 3-5 are comments I wrote earlier tonight while establishing the cost model. They were accurate when written and are invalidated by the very change they document — worth stating so the next reader does not trust them by default.

VERIFIED CLEAR (no action): myko uses no removed API — checked every removed pub item in hyphae PR #22 against myko's call sites (try_set, try_set_threshold, is_backed_up, with_metrics, on_slow_subscriber, hot_cells, log_hot_cells, register_cell, record_notify, update_name, mark_owned, reset_timing, notify_count, subscriber_count, avg_notify_time_ns: zero hits). The removed 'pub fn new()' belongs to impl CellMetrics, not Cell/CellMap. Cell/CellMap/WeakCell/WeakCellMap/MapQuery/MapExt/SelectExt/SwitchMapExt untouched. hyphae::profiling::pass/take_report survive unchanged (myko's only source-level hyphae-profiling use). Workspace features = ["scheduler"] and myko-core's profiling = ["dep:pprof", "hyphae/profiling"] both survive as-is.ebyDevxtrevor.a.sargent@gmail.comgcreatedx2026-07-21T20:05:06.952123Zbidx ce1a167ddcef460d8895d5b54267f7a9iprojectIdx de9416570df4447d8687b0103b13ae27ftaskIdx 3248f22b2f4b484a870cc7f5006c35b1jchangeTypecSEThitemTypegCommenticreatedAtx2026-07-21T20:05:06.952125Zbtxx$dca1399a-cb64-4cd5-9d05-ca127613e9cdhsourceIdgmalcolm