¦ditem©dbodyyßThe typegen migration established language-neutral public names, but the canonical machinery still aliases neutral Typegen* registrations to TS-specific Ts* registrations, and export_registered_ts_types() exports every linked TsExportRegistration because it carries no crate provenance. A downstream crate therefore writes dependency-owned ts-rs files as well as its own.

Refactor toward genuinely backend-agnostic internal machinery:

1. Define canonical neutral TypegenConstValue/Registration, TypegenModuleRegistration, and TypegenTypeRegistration with stable identity plus registration-site crate path. Compatibility Ts* names/macros may wrap the neutral layer, not vice versa.
2. Separate backend adapters from neutral type ownership. The TypeScript adapter owns the ts_rs::TS::export callback and associates it with a neutral type id; future renderers can attach their own adapters. Rust has no reflection, so the TS callback must not live in the neutral record.
3. Centralize crate-root normalization, exact ownership filtering, and catalog collection in the neutral typegen layer. Renderers consume the filtered catalog rather than independently filtering inventory.
4. Extract TS-specific generation still living in codegen/mod.rs (classes/imports/constants, formatting, generated file orchestration) into the TypeScript renderer where practical; keep operation parsing and language-neutral catalog logic outside it.
5. Add neutral sibling-crate ownership/catalog coverage (rship versus rship_core), a TS adapter-selection test proving only adapters for selected neutral registrations execute, and keep rendering assertions TS-specific.

Discovered while reproducing lv-ea59: register_typegen_type! currently delegates to register_ts_export!, shared_const! delegates to ts_const!, and linked dependencies' type exports are unconditionally executed. lv-ea59's exact crate-segment helper is a valid first neutral collector step; this task completes the architectural extraction for future language backends.gcreatedx2026-08-13T04:44:43.494240ZlcreatedByDevxtrevor.a.sargent@gmail.compcreatedByMachinegmalcolmbidx 1df11c2d014243cbb69444c32dfea3ecflabels‚gcodegengtypegenhprioritybP2iprojectIdx de9416570df4447d8687b0103b13ae27etitlex7Make typegen registration and catalogs backend-agnosticjchangeTypecSEThitemTypedTaskicreatedAtx2026-08-13T05:37:18.013550Zbtxx$e447f259-ebbb-4007-8eb2-faf31b7b062ahsourceIdgmalcolm