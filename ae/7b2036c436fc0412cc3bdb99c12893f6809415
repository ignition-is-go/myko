¦ditem©dbodyyâFrom the lv-fc26 rack crash loop: a boot killed mid-snapshot (OOM/cgroup kill) leaves its Postgres backend in ClientWrite still holding the myko_events lock; successive boots' snapshot queries then block until the 300s catch-up timeout cascades. Default TCP keepalives never reap the orphan.

Mitigated on the rack with PG-side tcp keepalives (idle=30/interval=10/count=3). Upstream fix candidates: set keepalives + statement_timeout/lock_timeout defaults on the consumer's reader connection (postgres.rs connect_consumer_client), so a dead client's backend dies promptly regardless of server config. Related perf: the DISTINCT ON snapshot scan itself is ~107s on 59M events (lv-ad85) â€” the longer the scan, the wider the convoy window.gcreatedx2026-07-21T05:57:42.220015ZlcreatedByDevxtrevor.a.sargent@gmail.compcreatedByMachinegmalcolmbidx 8c2f6fc1449d404480d7c4416d181cd6flabels€hprioritybP2iprojectIdx de9416570df4447d8687b0103b13ae27etitlexfpostgres consumer: killed boot leaves orphaned snapshot-query backend holding table lock (boot convoy)jchangeTypecSEThitemTypedTaskicreatedAtx2026-07-21T05:57:42.220023Zbtxx$2fbba6a5-8176-4c04-876a-a432cdaae76ahsourceIdgmalcolm