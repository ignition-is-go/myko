¦ditem¦dbodyycRETRACTING the fix direction in the body (2026-07-27, after design input from Trev). The body recommends splitting view/view_map_untyped into cfg'd decls bounding on ViewParams (wasm) vs ViewFactory (native). Do NOT do that.

Reason: Trev's concern is consumer ergonomics, plus an intended future where handlers may run CLIENT-side. Both make target_arch the wrong axis. Today 'compiled for wasm' and 'runs on the client' coincide, so cfg(target_arch) works as a proxy for 'no server here'. If handlers can run client-side that proxy breaks, and a per-target bound divergence doubles down on exactly the wrong distinction â€” it would have to be unwound immediately.

Better direction, and it subsumes this task: make the accessor layer backend-generic instead of target-gated. Querying::query_map etc. currently read through ServerScoped::__server_ctx() specifically; the shape that survives client-side handlers is an accessor with two impls (server-backed, client-backed). The client half largely exists already â€” MykoClient::watch_query_map (client/query_map.rs:24) is a working reactive query-map subscription and MykoClient is fully wasm-capable. The capability traits are sealed, so this accessor restructure is a non-breaking change for consumers.

Prerequisite either way: MykoServerContext needs to compile for wasm (see the sibling investigation â€” ~5 call sites: thread::spawn/sleep at server/context.rs:827, report_cache_stats.rs:81, entity_set_stats.rs:81, plus Instant::now() in persister.rs:67,129 and client_session.rs:260; no std::net/std::fs/tokio anywhere in server/). Once it does, ViewFactory naming MykoServerContext stops disqualifying Viewing from wasm and this task closes without any bound divergence at all.

Keeping this open as the tracking item for the 6 rship call sites, but the fix is now 'unblock via the accessor/backend work', not 'diverge the bound'.ebyDevxtrevor.a.sargent@gmail.comgcreatedx2026-07-27T23:26:29.844380Zbidx ba7f4eb5a7d24affb6b9bcee987d384eiprojectIdx de9416570df4447d8687b0103b13ae27ftaskIdx 3816c03250c342beb8e34b89ac5bf7fbjchangeTypecSEThitemTypegCommenticreatedAtx2026-07-27T23:26:29.844384Zbtxx$ac9d7a3a-3726-4077-99aa-ae3de7be08fbhsourceIdgmalcolm