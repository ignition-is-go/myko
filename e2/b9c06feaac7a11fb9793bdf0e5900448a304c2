¶ditem©dbodyy{`QueryHandler::build_view` is declared `#[cfg(not(target_arch = "wasm32"))]` with a native-only default (`None`) and no wasm counterpart ‚Äî unlike `ViewHandler::build_cell` and `QueryHandler::test_entity`, which both have a wasm default. A consumer that writes `fn build_view` in an un-gated `impl QueryHandler` gets E0407 on wasm32.

Structural, not an oversight in the same way as the view-context gap: `QueryBuildArgs`/`QueryBuildContext`/`FilteredCellMap` are all native-only types, and `QueryBuildContext` keeps fallible inherent `query`/`report` over an `Option<Arc<MykoServerContext>>` rather than adopting the capability traits.

Zero impact today: rship hand-writes no `build_view` (verified against rship-myko-6 @ feat/myko-6-migration, 2026-07-27). Filed so the asymmetry with `build_cell`/`test_entity` is a known gap rather than a surprise the first time a consumer writes one.gcreatedx2026-07-27T20:19:46.307458ZlcreatedByDevxtrevor.a.sargent@gmail.compcreatedByMachinegmalcolmbidx a1a607a54f084d1d9e9b6516205eb51bflabelsÅdwasmhprioritybP3iprojectIdx de9416570df4447d8687b0103b13ae27etitlex^QueryHandler::build_view has no wasm arm, so hand-written impls must be cfg-gated by consumersjchangeTypecSEThitemTypedTaskicreatedAtx2026-07-27T20:19:46.307462Zbtxx$d6ea414b-75eb-440e-83ab-ac5c8e94da11hsourceIdgmalcolm