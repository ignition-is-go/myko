$schema: 'https://moonrepo.dev/schemas/tasks.json'

inheritedBy:
  tag: 'docker-publish'

tasks:
  publish:
    options:
      cache: false
    deps:
      - 'build'
    command: '../../tools/scripts/docker-publish.sh'
